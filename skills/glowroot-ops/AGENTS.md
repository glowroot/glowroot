# Agent instructions — glowroot-ops

When the user asks about **Glowroot** (install, UI, traces, JVM, `-javaagent`, embedded/central):

1. Read **`SKILL.md`** in this directory.
2. Fetch live wiki from **https://github.com/glowroot/glowroot/wiki** (`wiki-index.md` for URLs).
3. Read **`ORG-CONTEXT.md`** if present (org-specific paths and stack).
4. Read other files only when relevant.
5. Analysis support only — no issue filing, no config changes.

Invoke: user says **glowroot-ops**, uses `/glowroot-ops`, or pastes **`PROMPT.md`**.

Setup: **`INSTALL.md`**.

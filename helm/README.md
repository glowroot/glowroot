# Helm charts

| Chart | Path | Notes |
| --- | --- | --- |
| glowroot-central | [`glowroot-central/`](glowroot-central/) | Official Central chart; Cassandra external |

Kubernetes ops (agent `collector.address`, `agent.id`):  
https://github.com/glowroot/glowroot/wiki/Kubernetes

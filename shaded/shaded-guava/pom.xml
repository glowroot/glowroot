<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
  xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>org.sonatype.oss</groupId>
    <artifactId>oss-parent</artifactId>
    <version>7</version>
  </parent>

  <groupId>org.informantproject.shaded</groupId>
  <artifactId>shaded-guava</artifactId>
  <!-- this is the last version of guava that supports jdk 5 -->
  <version>11.0.2-SNAPSHOT</version>

  <name>Guava (shaded for Informant)</name>
  <description>Guava (shaded for Informant)</description>
  <url>http://www.informantproject.org</url>
  <licenses>
    <license>
      <name>The Apache Software License, Version 2.0</name>
      <url>http://www.apache.org/licenses/LICENSE-2.0.txt</url>
      <distribution>repo</distribution>
    </license>
  </licenses>

  <prerequisites>
    <maven>2.2.1</maven>
  </prerequisites>

  <scm>
    <connection>scm:git:https://github.com/informantproject/shaded-guava.git</connection>
    <developerConnection>scm:git:git@github.com:informantproject/shaded-guava.git</developerConnection>
    <url>https://github.com/informantproject/shaded-guava</url>
  </scm>

  <dependencies>
    <dependency>
      <groupId>com.google.guava</groupId>
      <artifactId>guava</artifactId>
      <!-- this is the last version of guava that supports jdk 5 -->
      <version>11.0.2</version>
      <exclusions>
        <exclusion>
          <!--
            the shaded guava artifact is available to informant plugins
            but there is no need to make jsr305 available to plugins
          -->
          <groupId>com.google.code.findbugs</groupId>
          <artifactId>jsr305</artifactId>
        </exclusion>
      </exclusions>
    </dependency>
  </dependencies>

  <build>
    <plugins>
      <plugin>
        <artifactId>maven-shade-plugin</artifactId>
        <!--
          locally built 1.7.1 plus new shadeSourcesContent feature from
          https://jira.codehaus.org/browse/MSHADE-112
        -->
        <version>1.7.1-1-SNAPSHOT</version>
        <executions>
          <execution>
            <goals>
              <goal>shade</goal>
            </goals>
          </execution>
        </executions>
        <configuration>
          <createSourcesJar>true</createSourcesJar>
          <shadeSourcesContent>true</shadeSourcesContent>
          <relocations>
            <relocation>
              <pattern>com.google.common</pattern>
              <shadedPattern>org.informantproject.shaded.google.common</shadedPattern>
            </relocation>
          </relocations>
        </configuration>
      </plugin>
    </plugins>
  </build>
</project>

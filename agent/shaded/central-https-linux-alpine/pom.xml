<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
  xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
  xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>org.glowroot</groupId>
    <artifactId>glowroot-parent</artifactId>
    <version>0.13.7-SNAPSHOT</version>
    <relativePath>../../..</relativePath>
  </parent>

  <artifactId>glowroot-agent-central-https-linux-alpine</artifactId>

  <name>Glowroot Agent Central HTTPS Alpine Linux</name>
  <description>Glowroot Agent Central HTTPS Alpine Linux</description>

  <dependencies>
    <dependency>
      <groupId>com.github.trask.io.netty</groupId>
      <artifactId>netty-tcnative-boringssl-static-linux-alpine-x86_64</artifactId>
      <!-- need to stick to version known to work with gRPC 1.14.0,
        see https://github.com/grpc/grpc-java/blob/master/SECURITY.md#netty -->
      <version>2.0.12.Final</version>
    </dependency>
  </dependencies>

  <build>
    <plugins>
      <plugin>
        <!-- don't include glowroot LICENSE and NOTICE since this is just wrapped 3rd party lib -->
        <artifactId>maven-remote-resources-plugin</artifactId>
        <configuration>
          <skip>true</skip>
        </configuration>
      </plugin>
      <plugin>
        <artifactId>maven-dependency-plugin</artifactId>
        <executions>
          <execution>
            <goals>
              <goal>unpack</goal>
            </goals>
            <configuration>
              <artifactItems>
                <artifactItem>
                  <groupId>com.github.trask.io.netty</groupId>
                  <artifactId>netty-tcnative-boringssl-static-linux-alpine-x86_64</artifactId>
                </artifactItem>
              </artifactItems>
              <includes>META-INF/native/libnetty_tcnative.so</includes>
            </configuration>
          </execution>
        </executions>
      </plugin>
      <plugin>
        <artifactId>maven-shade-plugin</artifactId>
        <configuration>
          <dependencyReducedPomLocation>
            ${project.build.directory}/dependency-reduced-pom.xml
          </dependencyReducedPomLocation>
          <createSourcesJar>true</createSourcesJar>
          <shadeSourcesContent>true</shadeSourcesContent>
          <filters>
            <filter>
              <artifact>com.github.trask.io.netty:netty-tcnative-boringssl-static-linux-alpine-x86_64</artifact>
              <excludes>
                <exclude>META-INF/maven/**</exclude>
                <exclude>META-INF/io.netty.versions.properties</exclude>
                <!-- this need to be renamed so is pulled in below via transformers -->
                <exclude>META-INF/native/libnetty_tcnative.so</exclude>
              </excludes>
            </filter>
          </filters>
          <relocations>
            <relocation>
              <pattern>io.netty</pattern>
              <shadedPattern>org.glowroot.agent.shaded.io.netty</shadedPattern>
            </relocation>
          </relocations>
          <transformers>
            <transformer
              implementation="org.apache.maven.plugins.shade.resource.IncludeResourceTransformer">
              <resource>META-INF/native/liborg_glowroot_agent_shaded_netty_tcnative.so</resource>
              <file>${project.build.directory}/dependency/META-INF/native/libnetty_tcnative.so</file>
            </transformer>
          </transformers>
        </configuration>
        <executions>
          <execution>
            <goals>
              <goal>shade</goal>
            </goals>
          </execution>
        </executions>
      </plugin>
    </plugins>
  </build>
</project>
